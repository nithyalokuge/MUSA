<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Speech-to-text - Musa</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Lexend+Deca:wght@100..900&family=Mona+Sans:ital,wght@0,200..900;1,200..900&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/notyf@3/notyf.min.css">
    <script src="https://cdn.jsdelivr.net/npm/notyf@3/notyf.min.js"></script>
    <script src="/js/apply-settings.js"></script>
    <link rel="stylesheet" href="/css/style.css">
  </head>
  <body>
    <%- include("partials/header-sidebar-musa") %>

    <main>
      <section class="py-5">
        <div class="container">
          <div class="mb-5">
            <a href="index.html" class="back-to-home" aria-label="Back to home page">
              <i class="bi bi-arrow-90deg-left" aria-hidden="true"></i>
            </a>
            <h2 class="d-inline">Speech-to-text</h2>
            <p class="mt-4">
              Use this tool to follow along with museum docents during guided tours. It converts spoken words into on-screen text in real time.
            </p>
          </div>
          <div>
            <p class="mt-2 text-muted d-none" id="ios-helper">
              On iPhone or iPad? Tap in the box and use the mic on your keyboard to follow the tour guide.
            </p>
            <div id="output" class="output-box"></div>
            <div class="speech-buttons">
              <div class="d-flex gap-3">
                <button id="startBtn" class="speech-btn" aria-label="Start listening">
                  <i class="bi bi-mic" aria-hidden="true"></i>
                </button>
                <button id="stopBtn" class="speech-btn" aria-label="Pause" disabled>
                  <i class="bi bi-pause-fill" aria-hidden="true"></i>
                </button>
              </div>
              <div class="d-flex gap-3">
                <button id="copyBtn" class="speech-btn" aria-label="Copy to clipboard" disabled>
                  <i class="bi bi-clipboard" aria-hidden="true"></i>
                </button>
                <button id="clearBtn" class="speech-btn" aria-label="Clear text">
                  <i class="bi bi-trash3" aria-hidden="true"></i>
                </button>
              </div>
            </div>
          </div>
        </div>
      </section>
    </main>

    <!-- Footer -->
    <%- include("partials/footer") %>

    <script src="/js/speech-to-text.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>

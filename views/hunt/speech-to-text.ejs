<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Speech-to-Text - MUSA x The Hunt Museum</title>
    <link rel="icon" href="/img/favicon.png">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Lexend+Deca:wght@100..900&family=Noto+Sans:ital,wght@0,100..900;1,100..900&family=Onest:wght@100..900&display=swap">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.13.1/font/bootstrap-icons.min.css">
    <script src="/js/apply-settings.js"></script>
    <link rel="stylesheet" href="/css/the-hunt-museum-style.css">
  </head>
  <body>
    <%- include("../partials/header-hunt") %>

    <main>
      <section class="py-5">
        <div class="container">
          <div class="mb-4">
            <h2 class="text-center">Speech-to-Text</h2>
            <p class="mt-4">
              Use this tool to follow along with museum docents during guided tours. It converts spoken words into on-screen text in real time.
            </p>
          </div>
          <div class="mb-5">
            <p class="mt-2 text-muted d-none" id="ios-helper">
              On iPhone or iPad? Tap in the box and use the mic on your keyboard to follow the tour guide.
            </p>
            <div id="output" class="output-box"></div>
            <div class="speech-buttons">
              <div class="d-flex gap-3">
                <button id="startBtn" class="speech-btn" aria-label="Start listening">
                  <i class="bi bi-mic" aria-hidden="true"></i>
                </button>
                <button id="stopBtn" class="speech-btn" aria-label="Pause" disabled>
                  <i class="bi bi-pause-fill" aria-hidden="true"></i>
                </button>
              </div>
              <div class="d-flex gap-3">
                <button id="copyBtn" class="speech-btn" aria-label="Copy to clipboard" disabled>
                  <i class="bi bi-clipboard" aria-hidden="true"></i>
                </button>
                <button id="clearBtn" class="speech-btn" aria-label="Clear text" disabled>
                  <i class="bi bi-trash3" aria-hidden="true"></i>
                </button>
              </div>
            </div>
          </div>
          <div class="note">
            <label for="note" class="form-label">Notes</label>
            <textarea id="note" class="form-control" rows="4" placeholder="Write down anything."></textarea>
          </div>
        </div>
      </section>
    </main>

    <!-- Modal -->
    <%- include("../partials/modal") %>

    <!-- Footer -->
    <%- include("../partials/footer") %>

    <script src="/js/menu-hunt.js"></script>
    <script src="/js/active-link.js"></script>
    <script src="/js/speech-to-text.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
